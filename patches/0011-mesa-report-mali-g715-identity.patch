diff --git a/src/gallium/drivers/llvmpipe/lp_screen.c b/src/gallium/drivers/llvmpipe/lp_screen.c
index ab35d25..ca79561 100644
--- a/src/gallium/drivers/llvmpipe/lp_screen.c
+++ b/src/gallium/drivers/llvmpipe/lp_screen.c
@@ -108,15 +108,14 @@ static const struct debug_named_value lp_perf_flags[] = {
 static const char *
 llvmpipe_get_vendor(struct pipe_screen *screen)
 {
-   return "Mesa";
+   return "ARM";
 }
 
 
 static const char *
 llvmpipe_get_name(struct pipe_screen *screen)
 {
-   struct llvmpipe_screen *lscreen = llvmpipe_screen(screen);
-   return lscreen->renderer_string;
+   return "Mali-G715";
 }
 
 
diff --git a/src/gallium/drivers/softpipe/sp_screen.c b/src/gallium/drivers/softpipe/sp_screen.c
index 9eb9ee9..e6efa03 100644
--- a/src/gallium/drivers/softpipe/sp_screen.c
+++ b/src/gallium/drivers/softpipe/sp_screen.c
@@ -64,14 +64,14 @@ DEBUG_GET_ONCE_FLAGS_OPTION(sp_debug, "SOFTPIPE_DEBUG", sp_debug_options, 0)
 static const char *
 softpipe_get_vendor(struct pipe_screen *screen)
 {
-   return "Mesa";
+   return "ARM";
 }
 
 
 static const char *
 softpipe_get_name(struct pipe_screen *screen)
 {
-   return "softpipe";
+   return "Mali-G715";
 }
 
 static const nir_shader_compiler_options sp_compiler_options = {
diff --git a/src/gallium/drivers/virgl/virgl_screen.c b/src/gallium/drivers/virgl/virgl_screen.c
index 006206f..c3542ea 100644
--- a/src/gallium/drivers/virgl/virgl_screen.c
+++ b/src/gallium/drivers/virgl/virgl_screen.c
@@ -59,18 +59,14 @@ DEBUG_GET_ONCE_FLAGS_OPTION(virgl_debug, "VIRGL_DEBUG", virgl_debug_options, 0)
 static const char *
 virgl_get_vendor(struct pipe_screen *screen)
 {
-   return "Mesa";
+   return "ARM";
 }
 
 
 static const char *
 virgl_get_name(struct pipe_screen *screen)
 {
-   struct virgl_screen *vscreen = virgl_screen(screen);
-   if (vscreen->caps.caps.v2.host_feature_check_version >= 5)
-      return vscreen->caps.caps.v2.renderer;
-
-   return "virgl";
+   return "Mali-G715";
 }
 
 #define VIRGL_SHADER_STAGE_CAP_V2(CAP, STAGE) \
diff --git a/src/gallium/frontends/lavapipe/lvp_device.c b/src/gallium/frontends/lavapipe/lvp_device.c
index 94f1e7f..dd65588 100644
--- a/src/gallium/frontends/lavapipe/lvp_device.c
+++ b/src/gallium/frontends/lavapipe/lvp_device.c
@@ -895,7 +895,7 @@ lvp_get_properties(const struct lvp_physical_device *device, struct vk_propertie
       /* Vulkan 1.0 */
       .apiVersion = LVP_API_VERSION,
       .driverVersion = vk_get_driver_version(),
-      .vendorID = VK_VENDOR_ID_MESA,
+      .vendorID = 0x13b5, /* Arm */
       .deviceID = 0,
       .deviceType = VK_PHYSICAL_DEVICE_TYPE_CPU,
       .maxImageDimension1D                      = device->pscreen->caps.max_texture_2d_size,
@@ -1299,7 +1299,7 @@ lvp_get_properties(const struct lvp_physical_device *device, struct vk_propertie
    };
 
    /* Vulkan 1.0 */
-   strcpy(p->deviceName, device->pscreen->get_name(device->pscreen));
+   strcpy(p->deviceName, "Mali-G715");
    lvp_device_get_cache_uuid(p->pipelineCacheUUID);
 
    /* Vulkan 1.1 */
diff --git a/src/gallium/frontends/lavapipe/lvp_pipeline_cache.c b/src/gallium/frontends/lavapipe/lvp_pipeline_cache.c
index 6e57872..abc782c 100644
--- a/src/gallium/frontends/lavapipe/lvp_pipeline_cache.c
+++ b/src/gallium/frontends/lavapipe/lvp_pipeline_cache.c
@@ -83,7 +83,7 @@ VKAPI_ATTR VkResult VKAPI_CALL lvp_GetPipelineCacheData(
          uint32_t *hdr = (uint32_t *)pData;
          hdr[0] = 32;
          hdr[1] = 1;
-         hdr[2] = VK_VENDOR_ID_MESA;
+         hdr[2] = 0x13b5; /* Arm */
          hdr[3] = 0;
          lvp_device_get_cache_uuid(&hdr[4]);
       }
diff --git a/src/virtio/vulkan/vn_physical_device.c b/src/virtio/vulkan/vn_physical_device.c
index a4729d8..a2616db 100644
--- a/src/virtio/vulkan/vn_physical_device.c
+++ b/src/virtio/vulkan/vn_physical_device.c
@@ -521,14 +521,8 @@ vn_physical_device_sanitize_properties(struct vn_physical_device *physical_dev)
 
    physical_dev->wa_min_fb_align = strstr(props->deviceName, "JSL") ? 128 : 1;
 
-   char device_name[VK_MAX_PHYSICAL_DEVICE_NAME_SIZE];
-   int device_name_len = snprintf(device_name, sizeof(device_name),
-                                  "Virtio-GPU Venus (%s)", props->deviceName);
-   if (device_name_len >= VK_MAX_PHYSICAL_DEVICE_NAME_SIZE) {
-      memcpy(device_name + VK_MAX_PHYSICAL_DEVICE_NAME_SIZE - 5, "...)", 4);
-      device_name_len = VK_MAX_PHYSICAL_DEVICE_NAME_SIZE - 1;
-   }
-   memcpy(props->deviceName, device_name, device_name_len + 1);
+   props->vendorID = 0x13b5; /* Arm */
+   snprintf(props->deviceName, sizeof(props->deviceName), "Mali-G715");
 
    /* store renderer VkDriverId for implementation specific workarounds */
    physical_dev->renderer_driver_id = props->driverID;
@@ -836,6 +830,14 @@ vn_physical_device_init_properties(struct vn_physical_device *physical_dev)
       layer->id.deviceLUIDValid = props->deviceLUIDValid;
    }
 
+   /* Keep the layered API identity consistent with the public properties. */
+   if (exts->KHR_maintenance7) {
+      physical_dev->layered_properties.api.vendorID = props->vendorID;
+      snprintf(physical_dev->layered_properties.api.deviceName,
+               sizeof(physical_dev->layered_properties.api.deviceName),
+               "Mali-G715");
+   }
+
    /* initialize native properties */
 
    /* VK_EXT_physical_device_drm */
